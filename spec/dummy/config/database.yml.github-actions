development:
  adapter: postgresql
  database: db-blaster-dev

test:
  adapter: postgresql
  database: db-blaster-test
  pool:     20
  username: <%= ENV["POSTGRES_USER"] %>
  password: <%= ENV["POSTGRES_PASSWORD"] %>
